{
  "name": "@orchestrum/desktop",
  "version": "0.4.0",
  "private": true,
  "description": "Desktop shell for the local-first Orchestrum engineering OS.",
  "author": "Murat Ergin",
  "main": "main.js",
  "type": "commonjs",
  "scripts": {
    "dev": "electron .",
    "build": "electron-builder --dir",
    "dist": "electron-builder --publish never",
    "dist:mac": "electron-builder --mac dmg zip --publish never",
    "dist:linux": "electron-builder --linux AppImage tar.gz --publish never",
    "dist:win": "electron-builder --win nsis zip --publish never"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "local.orchestrum.desktop",
    "productName": "Orchestrum",
    "artifactName": "orchestrum-${version}-${os}-${arch}.${ext}",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      {
        "from": "../ui/.next/standalone",
        "to": "bundle/ui",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../ui/.next/static",
        "to": "bundle/ui/.next/static",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../ui/public",
        "to": "bundle/ui/public",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../service",
        "to": "bundle/service",
        "filter": [
          "dist/**/*",
          "package.json",
          "node_modules/**/*"
        ]
      },
      {
        "from": "../../packages/core",
        "to": "bundle/core",
        "filter": [
          "src/**/*",
          "package.json",
          "node_modules/**/*"
        ]
      },
      {
        "from": "../..",
        "to": "bundle",
        "filter": [
          "version.json",
          "CHANGELOG.md",
          "LICENSE",
          "EULA.md"
        ]
      }
    ],
    "asar": false,
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "identity": null,
      "hardenedRuntime": false,
      "gatekeeperAssess": false
    },
    "linux": {
      "target": [
        "AppImage",
        "tar.gz"
      ]
    },
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    }
  }
}
