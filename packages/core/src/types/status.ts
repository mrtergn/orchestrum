export type RunStatus = "pending" | "running" | "completed" | "failed" | "interrupted" | "cancelled";

export type StepStatus = "pending" | "running" | "ok" | "error" | "skipped" | "cached";

export type ApprovalStatus = "pending" | "approved" | "rejected" | "expired";
